{
  "name": "agent-relay",
  "version": "0.1.0",
  "description": "Dead-simple message mailboxes for AI agents",
  "license": "MIT",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --test-timeout=30000 test/*.test.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
